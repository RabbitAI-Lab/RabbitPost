{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for the main window (remote web shell).",
  "windows": ["main"],
  "permissions": ["core:default"]
}
